# Database configuration - Copy this file to .env.local and update with your values

# Local development mode with SQLite (default)
# Leave USE_POSTGRES unset or set to 'false' to use SQLite

# To use PostgreSQL, set USE_POSTGRES=true and configure PG_CONNECTION_STRING
# USE_POSTGRES=true
# PG_CONNECTION_STRING=postgresql://user:password@localhost:5432/choretracker

# For production with Neon (uncomment and update when deploying to Vercel)
# USE_POSTGRES=true
# PG_CONNECTION_STRING=postgres://user:password@endpoint.service.neon.tech/neondb